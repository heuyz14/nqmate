{
  "name": "nqmate",
  "private": true,
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "dev:web": "pnpm --dir apps/web dev",
    "build:web": "pnpm --dir apps/web build",
    "lint:web": "pnpm --dir apps/web lint",
    "test:api": "cd apps/api && PYTHONPATH=src python -m unittest discover -s tests"
  }
}
